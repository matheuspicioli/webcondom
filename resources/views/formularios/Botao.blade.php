<button class="btn {{ $classes }}" id="{{ $id ?? null }}" title="{{ $titulo ?? null }}" {{ $atributos ?? null }} data-toggle="{{ $toggle ?? null }}" data-target="{{ $target ?? null }}">
    <i class="{{ $icone ?? null }}"></i> {{ $texto }}</button>
