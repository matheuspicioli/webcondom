<button class="btn {{ $classes }}" id="{{ $id }}" title="{{ $titulo ?? null }}" {{ $atributos }} data-toggle="{{ $toogle ?? null }}" data-target="{{ $target ?? null }}">
    <i class="{{ $icone ?? null }}"></i> {{ $texto }}</button>
