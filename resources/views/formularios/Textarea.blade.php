<label for="{{ $id }}" class="control-label">{{ $texto }}</label>
<textarea name="{{ $nome }}" id="{{ $id }}" class="form-control pula" title="{{ $titulo ?? $texto ?? null }}" rows="{{ $rows }}" tabindex="{{ $tabindex ?? null }}">{{ $valor }}</textarea>